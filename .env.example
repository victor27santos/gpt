# Not needed in most cases: the front-end auto-detects the backend at
# http://<same-host-you-opened-the-app-from>:5000. Only set this if the
# backend runs on a different host/port than the front-end.
# VITE_API_BASE_URL=http://127.0.0.1:5000
