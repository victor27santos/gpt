ANTHROPIC_API_KEY=sk-ant-...
ANTHROPIC_MODEL=claude-sonnet-5

# Uncomment to restrict the backend to this machine only (default is 0.0.0.0,
# reachable from other devices on the same local network):
# HOST=127.0.0.1
# PORT=5000
